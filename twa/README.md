# GoHobby TWA Project

このディレクトリは、Bubblewrap CLIで生成されるTWAプロジェクト用です。

## セットアップ

```bash
npm install -g @bubblewrap/cli
cd ..
bubblewrap init --manifest ./public/manifest.json
```

## ビルド

```bash
cd twa
bubblewrap build
```

